[project]
name = "rpi-photo-booth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "luma-oled>=3.14.0",
    "pillow>=12.0.0",
    "python-escpos>=3.1",
]
